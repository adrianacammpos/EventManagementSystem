#include <iostream>

struct Event
{

    std::string name;
    std::string start_date;
    std::string finish_date;
    int max_capacity;
    int registration_list;
};

struct Person
{

    std::string name;
    std::string last_name;
    std::string discovery_method;
    int age;
    std::string career_interest;
};

int main(int argc, char *argv[])
{

    return 0;
}